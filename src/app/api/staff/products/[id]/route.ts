import { NextRequest, NextResponse } from "next/server";
import db from "@/lib/db";
import { requireAuthRole } from "@/lib/apiAuth";
const ROLES=["ADMIN","MANAGER","PRODUCT_MANAGER","STAFF"] as const;
export async function GET(_request:NextRequest,{params}:{params:Promise<{id:string}>}){const auth=await requireAuthRole([...ROLES]);if(auth.error)return auth.error;try{const{id}=await params;const product=await db.product.findUnique({where:{id},include:{category:{include:{subcategories:{orderBy:{position:"asc"}}}},subcategory:true,images:{orderBy:[{isPrimary:"desc"},{position:"asc"}]},variants:{orderBy:{position:"asc"},include:{attributes:true}},variantAttributes:{orderBy:{position:"asc"},include:{values:{orderBy:{position:"asc"}}}}}});if(!product)return NextResponse.json({error:"Product not found"},{status:404});return NextResponse.json({product:{...product,regularPrice:Number(product.regularPrice),salePrice:product.salePrice===null?null:Number(product.salePrice)}})}catch(e){console.error(e);return NextResponse.json({error:"Failed to load product"},{status:500})}}
export async function PUT(request:NextRequest,{params}:{params:Promise<{id:string}>}){const auth=await requireAuthRole([...ROLES]);if(auth.error)return auth.error;try{const{id}=await params;const body=await request.json();const existing=await db.product.findUnique({where:{id}});if(!existing)return NextResponse.json({error:"Product not found"},{status:404});const name=String(body.name??existing.name).trim();const regularPrice=Number(body.regularPrice??existing.regularPrice);const salePrice=body.salePrice===null||body.salePrice===""?null:Number(body.salePrice);if(!name)return NextResponse.json({error:"Product name is required"},{status:400});if(!Number.isFinite(regularPrice)||regularPrice<=0)return NextResponse.json({error:"Regular price must be greater than zero"},{status:400});if(salePrice!==null&&(!Number.isFinite(salePrice)||salePrice>=regularPrice))return NextResponse.json({error:"Sale price must be less than regular price"},{status:400});if(body.categoryId&&!await db.category.findUnique({where:{id:body.categoryId},select:{id:true}}))return NextResponse.json({error:"Invalid category"},{status:400});const data:any={name,sku:body.sku===""?null:(body.sku??existing.sku),description:body.description??null,shortDescription:body.shortDescription??null,regularPrice,salePrice,stockQuantity:Number.isFinite(Number(body.stockQuantity))?Number(body.stockQuantity):existing.stockQuantity,lowStockThreshold:Number.isFinite(Number(body.lowStockThreshold))?Number(body.lowStockThreshold):existing.lowStockThreshold,trackInventory:body.trackInventory??existing.trackInventory,allowBackorder:body.allowBackorder??existing.allowBackorder,categoryId:body.categoryId||existing.categoryId,subcategoryId:body.subcategoryId||null,status:body.status||existing.status,isActive:body.status?!["ARCHIVED","INACTIVE"].includes(body.status):existing.isActive,purchaseMethod:body.purchaseMethod||existing.purchaseMethod,isFeatured:body.isFeatured??existing.isFeatured,isBestseller:body.isBestseller??existing.isBestseller,isNewArrival:body.isNewArrival??existing.isNewArrival,isComingSoon:body.isComingSoon??existing.isComingSoon,material:body.material??null,color:body.color??null,finish:body.finish??null,shape:body.shape??null,pattern:body.pattern??null,style:body.style??null,mountingType:body.mountingType??null,usageLocation:body.usageLocation??null,careInstructions:body.careInstructions??null,warranty:body.warranty??null,packagingType:body.packagingType??null,packagingDimensions:body.packagingDimensions??null,includedItems:body.includedItems??null,dimensionUnit:body.dimensionUnit??existing.dimensionUnit,weightUnit:body.weightUnit??existing.weightUnit,capacityUnit:body.capacityUnit??existing.capacityUnit,height:body.height??null,width:body.width??null,length:body.length??null,depth:body.depth??null,diameter:body.diameter??null,weight:body.weight??null,capacity:body.capacity??null,allowCustomSize:body.allowCustomSize??existing.allowCustomSize,customSizeUnit:body.customSizeUnit??existing.customSizeUnit,customSizeMinWidth:body.customSizeMinWidth??null,customSizeMinLength:body.customSizeMinLength??null,customSizeMinHeight:body.customSizeMinHeight??null,customSizeMaxWidth:body.customSizeMaxWidth??null,customSizeMaxLength:body.customSizeMaxLength??null,customSizeMaxHeight:body.customSizeMaxHeight??null,customSizePricingMethod:body.customSizePricingMethod??existing.customSizePricingMethod,customSizeRequiresApproval:body.customSizeRequiresApproval??existing.customSizeRequiresApproval,seoTitle:body.seoTitle??null,seoDescription:body.seoDescription??null};const product=await db.product.update({where:{id},data});await db.auditLog.create({data:{action:"UPDATE",entity:"PRODUCT",entityId:id,details:{name:product.name,source:"STAFF"}}});return NextResponse.json({product:{...product,regularPrice:Number(product.regularPrice),salePrice:product.salePrice===null?null:Number(product.salePrice)}})}catch(e:any){console.error(e);if(e?.code==="P2002")return NextResponse.json({error:"That SKU is already used by another product"},{status:409});return NextResponse.json({error:"Failed to update product"},{status:500})}}
